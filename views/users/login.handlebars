<div class="form-area">
    <form action="/users/login" method="post">
    {{#if messages.message}}
        <div class="message">
            <i class="bi bi-info-circle fa-4x" ></i>
            {{messages.message}}
        </div>  
    {{/if}}
    <h1>Login:</h1>
    <div class="email-area">
        <label for="email">Digite seu Email:</label><br>
        <input type="text" name="email" placeholder="Seu Email..." autocomplete="on">
    </div>
    <div class="password-area">
        <label for="password">Digite sua senha:</label><br>
        <input type="password" name="password" placeholder="Sua senha..."><br>
    </div>
        <input type="submit" value="Entrar">
            <a href="/">Homepage</a>
            <a href="/auth/register">Não está cadastrado?</a>
    </form>    
</div>